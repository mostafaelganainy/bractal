/* eslint-disable import/prefer-default-export */
import styled from 'styled-components';
import { Dropdown } from 'semantic-ui-react';

export const AykDropdown = styled(Dropdown)`
  font-weight: 900;
  font-size: 12px;
  color: #000000;
`;
